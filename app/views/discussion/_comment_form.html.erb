<% form_for :comment, @comment, :url => "/discussion/comment/#{@comment.id if @comment}" do |f| %>
	<%= f.hidden_field :discussion_id, :value => @discussion.id %>
	<%= textile_editor 'comment', 'body' -%><br/><br/>
	<%= textile_editor_initialize -%>
	<%= submit_tag %>
<% end %>